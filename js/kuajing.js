(function($){
	$(document).ready(function() {
		// $('select.dropdown-select').each(function() {
		// 	var className = 'dropdown ' + $(this).attr('class');
		// 	$(this).easyDropDown({
		// 		wrapperClass: className,
		// 		width: '100px'
		// 	});
		// });

	})
})(jQuery);